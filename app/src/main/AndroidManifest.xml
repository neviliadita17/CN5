<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cn5">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu"></activity>
        <activity android:name=".NewsInsert" />
        <activity android:name=".NewsDetail" />
        <activity android:name=".MainActivity" /> <!-- <activity android:name=".Show" /> -->
        <activity android:name=".InsertNews" />
        <activity android:name=".MainNews" /> <!-- <activity android:name=".MainBerita" /> -->
        <activity android:name=".CreateAccount" />
        <activity android:name=".DetailAccount" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".LandingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>